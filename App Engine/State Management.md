
Instances are stateless by nature, thus local in-memory data is not stored between sessions and requests

*What happens when requests are going to different instances?*

Thus we need to avoid using local memory, instead, use external storage systems
