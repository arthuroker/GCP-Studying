
*Before VPC Egress*

By default, serverless services can't reach private resources

The Google Managed Network (for serverless resources) is separate from a Cloud VPC Network

*Direct VPC Egress versus Serverless VPC Access*

