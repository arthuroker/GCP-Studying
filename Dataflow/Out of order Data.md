
Windows + watermarks + triggers

These are all ways to handle out of order data

Organize data into windows, determine when data was submitted/processed to Dataflow, allow stragglers to catch up, and decide when to emit the final results

