
Skew detection requires a valid training data baseline, because by definition "skew" compares training data to serving data

Baseline data set must match the training dataset used to train the deployed model

Process

1) Train model
2) Deploy
3) Upload same training data in model monitoring
4) Enable skew detection 