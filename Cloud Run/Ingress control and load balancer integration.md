
*Problem* - Cloud Run has two entry points

Example:

You have a load balancer that has Cloud Armor. You have a custom domain, but users can access the run.app endpoint directly. Cloud Armor is only applied at the load balancer level, thus not security protection