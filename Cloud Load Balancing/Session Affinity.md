
Imagine a request where users need to send requests to the same backend server to maintain a session (stateful workload)

Layer 7 is best at this, because it has info like cookies and application data and HTTPS information
	aka HTTPS load balancer

